import { createContext } from 'react';

export const PopulationContext: any = createContext(undefined);
export const MenuContext: any = createContext(undefined);
export const AboutContext: any = createContext(undefined);